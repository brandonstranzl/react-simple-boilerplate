const joel = {
  random: "randomnumber",
  mindset: "fixed",
  feeling: "blah",
  health: "dirty"
}

console.log(joel.length)

console.log(Object.keys(joel))

console.log(Object.getOwnPropertyNames(joel))

array = [1,2]
arr2 = [5,7]

console.log(array.forEach === arr2.forEach)